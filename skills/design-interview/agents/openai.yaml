interface:
  display_name: "Design Interview"
  short_description: "Resolve material design choices one question at a time"
  default_prompt: "Use $design-interview to interrogate the unresolved design choices before implementation."
policy:
  allow_implicit_invocation: false
